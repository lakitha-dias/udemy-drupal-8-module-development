{{ attach_library('module_hero/hero-style') }}

<h4 class="animated bounce">{{ title }}</h4>

<ol>
	{% for item in items %}
		<li class="animated {{ random(['bounce', 'flash', 'rubberBand']) }}">{{ item.name }}</li>
	{% endfor %}
</ol>